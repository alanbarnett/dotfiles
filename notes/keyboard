switches I want to try
	
my planck
	keycaps
		dsa
			grey inside and layers
			black outside and spacebars
	switches
		kailh box pale blue
	case
		OLKB EOTW sandwich case

next in line plancks
	comfy, quiet
		keycaps
		switches
		case

qmk
	build/compile instructions
		make <keyboard_folder>:<keymap>:<target> [options]
			<keyboard_folder>	= planck/rev5
			<keymap>			= name of keymap (ex. default, mine)
			<target>			= dfu
			[options]
				COLOR=false
					turns off color output?
				SILENT=true
					turns off output besides errors/warnings
				VERBOSE=true
				EXTRAFLAGS=-E
					preprocess code without doing compiling
					useful to debug #define commands
				-j3
		rules.mk options
			MOUSEKEY_ENABLE
			EXTRAKEY_ENABLE
				system and audio control key codes
			COMMAND_ENABLE
				lshift+rshift+<key>
				what magic commands are possible?
			SLEEP_LED_ENABLE
				LED breathing while computer is sleeping
				untested, unstable
			NKRO_ENABLE
				enables n-key rollover
				add #define FORCE_NKRO to config.h
				or, bind MAGIC_TOGGLE_NKRO and use the magic shortcut
			BACKLIGHT_ENABLE
				think this is just for leds
				Timer1 on ports B5, B6, B7
			MIDI_ENABLE
				untested
				bind MI_ON and MI_OFF
				more information in quantum/quantum.c
			UNICODE_ENABLE
				send unicode symbols via UC(<unicode>) in the keymap
				only codes up to 0x7FFF
			UNICODEMAP_ENABLE
				send unicode symbols via X(<unicode>) in the keymap
				codes up to 0xFFFFFFFF
					mac supports up to 0xFFFF
					linux ibus supports up to 0xFFFFF
			BLUETOOTH_ENABLE
				uses D2 and D3 pins
			AUDIO_ENABLE
				output audio on C6 pin
			FAUXCLICKY_ENABLE
				uses buzzer to sound like cherry blues, uses the C6 pin
			VARIABLE_TRACE
				for debugging variables
			API_SYSEX_ENABLE
				uses Quantum SYSEX API to send strings
				comsumes 5390 bytes (wow)
			KEY_LOCK_ENABLE
				enables a hold key, I don't want this

